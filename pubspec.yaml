name: dewy_app
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.8.1

dependencies:
  flutter:
    sdk: flutter



  supabase_flutter: ^2.0.0
  http: ^1.1.0
  shimmer: ^3.0.0  # Add this line

  cupertino_icons: ^1.0.8
  flutter_svg: ^2.0.9
  flutter_native_splash: ^2.4.7

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/logo.svg

flutter_native_splash:
  color: "#78563C"   # Dark brown color
  android: true
  ios: true
  fullscreen: true

